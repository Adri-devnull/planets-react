import styled from 'styled-components';
import { COLORS } from '../../constants/designValues';

const StyledMenu = styled.nav`
	display: flex;
	align-items: center;
	justify-content: space-between;
	color: ${COLORS.title};
`;

export { StyledMenu };
